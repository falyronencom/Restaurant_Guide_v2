library;

/// Открытие ссылки в новой вкладке.
///
/// Реализация выбирается по платформе. Это не украшательство: `dart:js_interop`
/// недоступен на Dart VM, а `flutter test` работает именно там. Прямой импорт
/// js-interop делает НЕТЕСТИРУЕМЫМ весь виджет и всё, что его подключает —
/// панель модерации так и жила, пока это не всплыло.
///
/// Через `window.open`, а не `url_launcher`, хотя пакет в зависимостях есть:
/// в проекте так уже было сделано открытие Яндекс.Карт, и этот путь проверен
/// в работе. Заводить второй способ ради того же действия незачем.
export 'open_url_stub.dart' if (dart.library.js_interop) 'open_url_web.dart';
