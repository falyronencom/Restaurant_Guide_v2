import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Admin app smoke test', (WidgetTester tester) async {
    // Placeholder — real tests will be added as screens are implemented
    expect(1 + 1, equals(2));
  });
}
